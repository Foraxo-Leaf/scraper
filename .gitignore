# Python general
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environment
.env
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs and editors
.idea/
.vscode/
*.swp
*~
*.sublime-project
*.sublime-workspace

# Archivos y directorios específicos del proyecto
# (ejemplos, ajustar según necesidad)
*.log
*.sqlite3
*.db # Para la base de datos del scraper si se usa ese nombre genérico

# Ignorar directorios y archivos generados dentro de AR/
AR/db/
AR/logs/
AR/output/
AR/output_package/
AR/docs/sample_pdfs/
AR/output/state.json
AR/output/test_results.json

# Si tienes otros directorios de países (BO, CL, etc.) y generan salidas similares,
# puedes añadir reglas para ellos también, o usar patrones más genéricos si aplica.
# Ejemplo para ignorar carpetas 'output' en cualquier subdirectorio de un país:
# **/output/
# Sin embargo, ser específico como arriba para AR/ es más seguro.

# Ignorar otros archivos específicos del proyecto que no deben ser versionados
# <CODE>/ # Si esta carpeta es generada y no debe subirse
# analysis.json # Si es un archivo temporal
# ... etc.

# Ignorar directorio 'scraper' si ya no es relevante o es un submodulo no gestionado
scraper/ 